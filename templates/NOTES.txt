1. Deploy the chart:
   helm install {{ .Release.Name }} .

2. (Optional) Forward the UDP port to test locally:
   kubectl port-forward service/{{ include "tftp-server.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} --address 0.0.0.0

3. Use your favorite TFTP client to upload/download from the service.
